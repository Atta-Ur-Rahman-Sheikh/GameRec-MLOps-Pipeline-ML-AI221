# Runtime dependencies for the FastAPI service and Prefect retraining flow.
# Pinned to currently-installed major versions; bump as needed.

# --- Web service ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-multipart>=0.0.6

# --- Numerical / ML ---
numpy>=1.26,<2.2
pandas>=2.1,<2.3
scipy>=1.11,<2.0
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
joblib>=1.3,<2.0

# --- Orchestration ---
prefect>=3.0,<4.0
